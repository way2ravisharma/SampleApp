import {StyleSheet} from 'react-native';
import {PrimeWhite} from '../../../constants/styles.constants';

export const styles = StyleSheet.create({
  spinnerTextStyle: {
    fontSize: 20,
    marginTop: 10,
    color: PrimeWhite,
    textAlign: 'center',
  },
});
